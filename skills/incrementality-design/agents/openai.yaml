interface:
  display_name: "Incrementality Design"
  short_description: "Incrementality Design"
  brand_color: "#173F35"
  default_prompt: "Use Incrementality Design on this marketing decision. Diagnose current evidence, apply the relevant theory, challenge platform bias, then recommend one measurable action."
policy:
  allow_implicit_invocation: true
